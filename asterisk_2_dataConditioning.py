"""
This file will...

1) normalize the translation data by hand span
2) run a moving average filter over the data
3) remove clusters of data 
4) resample the data to be x number of samples

"""

import csv



def normalize_data(val, hand_span):
    return val / hand_span

def filter_data(data):
    #moving average
    filtered_data = data

    return filtered_data

def remove_clusters(data):
    #remove clusters
    declustered_data = data

    return declustered_data

def resample_data(data):
    #resample data
    resampled_data = data

    return resampled_data




